apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - local-path-provisioner
  # cilium, metrics-server, and cloudflared are added in a follow-up commit,
  # timed with the flannel -> Cilium cutover (see repo history / plan notes).
