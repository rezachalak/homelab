apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: adguard-home
  namespace: adguard
spec:
  parentRefs:
    - name: gateway
      namespace: kube-system
  hostnames:
    - adguard.rezoreyz.lan
  rules:
    - backendRefs:
        - name: adguard-home
          port: 80
